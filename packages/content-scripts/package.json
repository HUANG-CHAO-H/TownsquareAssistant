{
  "name": "content-scripts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite build --config vite.config.ts --mode development",
    "build": "vite build --config vite.config.ts"
  }
}
